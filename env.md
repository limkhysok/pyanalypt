## For Windows (PowerShell/CMD):
# 1. Create the virtual environment named 'venv'
python -m venv venv

# 2. Activate the environment
.\venv\Scripts\activate

# 3. Install the libraries from the file
pip install -r requirements.txt


## For Mac/Linux:
# 1. Create the virtual environment named 'venv'
python -m venv venv

# 2. Activate the environment
.\venv\Scripts\activate

# 3. Install the libraries from the file
pip install -r requirements.txt